"""
Spyder Editor

This is a temporary script
ASSIGNMENT 1.1

Area of circle program-

radius= float(input("Enter the radius of the circle:"))
area = 3.14*radius*radius
print ("area of circle is:",area)


why do we have the name float?
A float is a data type composed of a number that is not an integer , because it includes a fraction represented in decimal form.


Arithmetic operators -

a=10
b=20
add=a+b
sub=a-b
mul=a*b
mod=a%b
print(add)
print(sub)
print(mul)
print(mod)

assignment 1.2 

practice comparision and assignment operators with different values 4sets

print(3 > 2)
print(3 < 2)
print (3 == 3)
print (3 != 2)

assignment operators 

random_number=10
random_number+=20
print(random_number)

random_number=10
random_number-=2
print(random_number)

random_number=5
random_number*=10
print(random_number)

random_number=9
random_number//=2
print(random_number)


practice If and Else statements with the following 
 1.If it snows I willgo out Else I will not
 
 name="snow"
 if name == "snows":
     print(" i will go")
 else:
     print("i will not")
 
    2. If it rains frogs will dance
    
    name = "rains"
    if name== 'rains':
        print (" frogs will dance")
    else:
        print("frogs will not dance")

    
assignment 2.0

functions /methods of dictionary from w3scools

PYTHON DICTIONARY IN-BUILT FUNCTIONS

len() , any() ,all() , sorted()

PYTHON DICTIONARY IN -BUILT METHODS-

Keys(), values(), items(), get(), clear(), cpoy(), pop(), popitem(), fromkeys(), updae()


create a set - refer from w3schools

a={1,4,1.2,'aaa','bb','cccc',9}
b=set({1,4,'aa','bbb'})
c=set()
print(a)
print(b)
print(c)

create ur own calculator using If conditions and functions

 def add (x,y): 
 
 
 def add (x,y): 
     """""this function adds 2 numbers""""""
     return x+y
 
     def subtract (x,y): 
         """""this function subtracts 2 numbers"""""
         return x-y
     
         def multiply (x,y): 
             """""this function multiplys 2 numbers"""""
             return x*y
             
             def division (x,y): 
                   """""this function divides 2 numbers"""
                  return x/y
 
 print("select operation.")

print("1.Add") 
print("2.subtract") 
print("3.multiply") 
print("4.division") 
choice=input ("enter choice 1/2/3/4:")

num1 = int(input("Enter first number:"))
num2 = int (inout ("Enter the second number:"))


if choice == '1':
    num2= int (input ("Enter the second number: "))

if choice == '1': print (numi, "+", num2, " ", add (num1, num2))

elif choice == 2: print (numl, "-", num2, "", substract (numl, num2))

elif choice == 3:

print (numl, "x", num2,"=" , multiply (numl, num2))

elif :e==^ prime 4^ prime : print (numl, prime/^ prime prime , num2,"=" , divide (numl, num2))

else: print ("Invalid input")


assignment 2.2

1. int i;
for (i = 0;i<=5; i+=2)
{
}
    break;

2 .tuplex = (4, 6, 2, 8, 3, 1) 
print(tuplex)
tuplex = tuplex + (9,)
print(tuplex)

3.int i;
for (i = 0;; i++)
  if (thereIsAReasonToBreak(i))
    break;

